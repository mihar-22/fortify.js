// Which driver?

// Option -c will clear cache allow a new connection.

// Ask for database connection if not cached.

// Ask for naming strategy if not cached.

// Bootstrap application.

// Establish connection.

// Build Migrator.

// Pull in migrations from '../../dist/src/modules/database/migrations'

// Return it to call up or down.

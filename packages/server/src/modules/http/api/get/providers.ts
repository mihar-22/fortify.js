import { Request } from '../../../http/Request';
import { Response } from '../../../http/Response';

export default (req: Request, res: Response) => {
  // GET -> /providers

  // returns currently configured providers, maybe another way??
};

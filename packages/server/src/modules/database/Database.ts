export interface Database {
  // create<T>(collection: string, data: T): T
  // read(collection, filter) -> if filter contains nested object we can assume its a join (key).
  // update(collection, id, data)
  // delete(collection, id)
}

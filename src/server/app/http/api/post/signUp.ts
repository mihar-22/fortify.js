import { Request } from '../../../../services/routing/Request';
import { Response } from '../../../../services/routing/Response';

export default (req: Request, res: Response) => {
  // extract data from route
  // validate
  // call signUp on auth manager
  // call onSignUp event
  // return response
};

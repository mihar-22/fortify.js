import { Container } from 'inversify';

describe('Mail', () => {
  describe('Module', () => {
    let container: Container;

    const loadModule = async () => {
    };

    test('hm', () => {
    });
  });
});
